import { HeaderSizeDirective } from './header-size.directive';

describe('HeaderSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderSizeDirective();
    expect(directive).toBeTruthy();
  });
});
